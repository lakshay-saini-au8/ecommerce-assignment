import React from "react";
import Logo from "../Logo/Logo";
const Header = () => {
  return (
    <div className="Header" style={{ padding: "0 40px" }}>
      <Logo />
    </div>
  );
};

export default Header;
